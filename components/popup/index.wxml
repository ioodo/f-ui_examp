<view
  class="f-popup {{ visible ? 'f-popup-show' : ''}}"
  bindtap="onPopupTap"
>
  <view
    class="f-popup-content {{ contentClass }} {{ contentClassName }}"
  >
    <slot></slot>
  </view>
</view>